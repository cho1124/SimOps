# SimOps Game Core

This embedded Unity package contains the managed `.NET Standard 2.1` binary built from `src/SimOps.Game.Core`.

Run `scripts/Build-GameCorePackage.ps1` from the repository root after changing Game Core code. The script builds Release and copies the DLL into `Runtime/Plugins`.
